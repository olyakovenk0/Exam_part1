import {addRating, submitFeedbackForm} from '../support/Helper';


describe('Authorisation', () => {
  
    it('check if user is authorised after login in', () => {
        submitFeedbackForm();
    })
  })
